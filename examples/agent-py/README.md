# Python Agent Example

Run the sample:

```bash
AGENT_ID=<agent-id> TENANT_ID=acme python3 examples/agent-py/main.py
```

Optional auto-approve mode:

```bash
AGENT_ID=<agent-id> TENANT_ID=acme AUTO_APPROVE=1 APPROVER_ID=sec-lead-1 python3 examples/agent-py/main.py
```
